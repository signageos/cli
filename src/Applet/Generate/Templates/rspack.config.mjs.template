import { rspack } from '@rspack/core';
import { defineConfig } from '@rspack/cli';
import SignageOSPlugin from '@signageos/webpack-plugin';

export default defineConfig({
  entry: {
		main: './src/index', // Universal configuration works for .js and .ts
	},
	resolve: {
		extensionAlias: {
			'.js': ['.ts', '.tsx', '.js'], // First search for .ts, than .js
		},
		extensions: ['.ts', '.tsx', '.js'],
	},
	target: ['web', 'es5'],
	output: {
		filename: 'index.js',
	},
	// @signageos/front-applet is a prebuilt ~1.2 MiB bundle that every applet includes, so the
	// default 244 KiB budget can never be met by an applet author. Warning about it on every
	// build only teaches them to ignore build output.
	performance: {
		hints: false,
	},
	module: {
		rules: [
			{
				test: /^(.(?!.module.css))*.css$/,
				use: ['style-loader', 'css-loader'],
			},
			{
				test: /\.(tsx?|js)$/,
				loader: 'builtin:swc-loader',
				options: {
				  jsc: {
					parser: {
					  syntax: 'typescript',
					},
					target: 'es5',
				  },
				},
			},
		],
	},
	plugins: [
		new rspack.HtmlRspackPlugin({
			template: 'public/index.html',
		}),
		new SignageOSPlugin(),
	],
  infrastructureLogging: {
		level: 'warn',
	},
});
